const a = 4;
const b = 2;

if (a < b) {
  console.log(`${a} lebih kecil dari ${b}`);
} else if (a === b) {
  console.log(`${a} sama dengan ${b}`);
} else {
  console.log(`${a} lebih besar dari ${b}`);
}
